<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>




    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconoappna"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconoappna"
        android:supportsRtl="true"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:theme="@style/Theme.App_Pedidos"
        tools:targetApi="31">

        <!-- LoginActivity se convierte en la actividad principal -->
        <activity
            android:name=".ui.Login.LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.App_Pedidos.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Otras actividades de tu aplicación -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.App_Pedidos.NoActionBar" />

        <activity
            android:name=".ui.Inspeccion.InspeccionVehicularActivity"
            android:exported="false"
            android:label="Inspección de vehículo"
            android:theme="@style/Theme.App_Pedidos" />

        <activity
            android:name=".ui.Inspeccion.InspeccionHoyActivity"
            android:exported="false"
            android:label="Inspección de hoy"
            android:theme="@style/Theme.App_Pedidos" />

        <activity
            android:name=".ui.Pedido.DetallePedidoActivity"
            android:label="DetallePedidoActivity"
            android:exported="true" />

        <activity
            android:name=".ui.Pedido.Detalle_Actualizaciones"
            android:label="DetalleActualizacionesActivity"
            android:exported="true" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
